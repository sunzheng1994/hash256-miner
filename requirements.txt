# Core (CPU + chain)
web3>=6.0.0
pycryptodome>=3.20.0
typer>=0.12.0
rich>=13.0.0
tqdm>=4.66.0
numpy>=1.24.0

# Development
pytest>=8.0.0

# GPU: install ONE of the following matching your CUDA driver / runtime
# pip install cupy-cuda12x
# pip install cupy-cuda11x
# See INSTALL.md
